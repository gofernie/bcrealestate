---
export const prerender = false;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Guess the Asking Price | BC Real Estate</title>
    <meta
      name="description"
      content="Test your BC real estate price knowledge using real active listings."
    />
    <style>
      :root {
        --bg: #f4f1eb;
        --card: #fff;
        --ink: #17212b;
        --muted: #65717c;
        --line: rgba(23, 33, 43, 0.12);
        --accent: #163b5d;
        --good: #1e7a4f;
        --warn: #9a6419;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        min-height: 100vh;
        color: var(--ink);
        background:
          radial-gradient(circle at top, rgba(22, 59, 93, 0.08), transparent 32rem),
          var(--bg);
      }

      button, input, select { font: inherit; }

      .shell {
        width: min(1120px, calc(100% - 28px));
        margin: 0 auto;
        padding: 22px 0 54px;
      }

      .topbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
        margin-bottom: 22px;
      }

      .brand {
        color: inherit;
        text-decoration: none;
        font-weight: 850;
        letter-spacing: -0.03em;
      }

      .brand span { color: var(--accent); }

      .scoreboard {
        display: flex;
        gap: 8px;
      }

      .score-pill {
        min-width: 88px;
        padding: 9px 12px;
        border: 1px solid var(--line);
        border-radius: 999px;
        background: rgba(255,255,255,.78);
        text-align: center;
        box-shadow: 0 8px 24px rgba(23,33,43,.05);
      }

      .score-pill small {
        display: block;
        color: var(--muted);
        font-size: .68rem;
        font-weight: 750;
        text-transform: uppercase;
        letter-spacing: .08em;
      }

      .score-pill strong { font-size: .95rem; }

      .game {
        display: grid;
        grid-template-columns: minmax(0, 1.2fr) minmax(300px, .8fr);
        overflow: hidden;
        border: 1px solid var(--line);
        border-radius: 28px;
        background: var(--card);
        box-shadow: 0 24px 80px rgba(23,33,43,.10);
      }

      .listing-photo {
        position: relative;
        min-height: 660px;
        background: #dfe4e7;
      }

      .listing-photo img {
        width: 100%;
        height: 100%;
        min-height: 660px;
        object-fit: cover;
        display: block;
      }

      .photo-shade {
        position: absolute;
        inset: 0;
        background: linear-gradient(180deg, transparent 50%, rgba(8,16,23,.72));
        pointer-events: none;
      }

      .listing-caption {
        position: absolute;
        left: 24px;
        right: 24px;
        bottom: 22px;
        color: #fff;
      }

      .listing-caption h2 {
        margin: 0 0 6px;
        font-size: clamp(1.35rem, 2.6vw, 2.1rem);
        line-height: 1.05;
        letter-spacing: -.03em;
      }

      .listing-caption p { margin: 0; opacity: .85; }

      .panel {
        display: flex;
        flex-direction: column;
        padding: clamp(24px, 4vw, 44px);
      }

      .eyebrow {
        margin: 0 0 10px;
        color: var(--accent);
        font-size: .75rem;
        font-weight: 850;
        text-transform: uppercase;
        letter-spacing: .12em;
      }

      h1 {
        margin: 0;
        font-size: clamp(2rem, 4vw, 3.6rem);
        line-height: .98;
        letter-spacing: -.055em;
      }

      .intro {
        margin: 18px 0 24px;
        color: var(--muted);
        line-height: 1.6;
      }

      .meta {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 26px;
      }

      .meta span {
        padding: 8px 11px;
        border: 1px solid var(--line);
        border-radius: 999px;
        background: #fafafa;
        font-size: .82rem;
        font-weight: 700;
      }

     .price-question {
  margin: 0 0 10px;
  color: var(--muted);
  font-size: .74rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: .08em;
}

:global(.price-options) {
  display: grid;
  gap: 12px;
}

:global(.price-option) {
  position: relative;
  width: 100%;
  min-height: 62px;
  padding: 14px 18px;
  border: 1px solid rgba(23, 33, 43, 0.14);
  border-radius: 16px;
  background: #ffffff;
  color: var(--ink);
  cursor: pointer;
  font-size: 1.12rem;
  font-weight: 850;
  line-height: 1;
  text-align: center;
  letter-spacing: -0.02em;
  box-shadow: 0 8px 22px rgba(23, 33, 43, 0.05);
  transition:
    transform 0.14s ease,
    border-color 0.14s ease,
    background 0.14s ease,
    box-shadow 0.14s ease;
}

:global(.price-option)::after {
  content: "Choose";
  position: absolute;
  top: 50%;
  right: 16px;
  transform: translateY(-50%);
  color: var(--muted);
  font-size: 0.65rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  opacity: 0;
  transition: opacity 0.14s ease;
}

:global(.price-option:hover) {
  transform: translateY(-2px);
  border-color: var(--accent);
  background: rgba(22, 59, 93, 0.025);
  box-shadow: 0 12px 28px rgba(23, 33, 43, 0.1);
}

:global(.price-option:hover)::after {
  opacity: 1;
}

:global(.price-option:focus-visible) {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 4px rgba(22, 59, 93, 0.12);
}

:global(.price-option:disabled) {
  cursor: default;
  transform: none;
  opacity: 0.72;
}

:global(.price-option:disabled)::after {
  display: none;
}

:global(.price-option.is-correct) {
  border-color: var(--good);
  background: rgba(30, 122, 79, 0.1);
  color: var(--good);
  opacity: 1;
  box-shadow: 0 0 0 3px rgba(30, 122, 79, 0.12);
}

:global(.price-option.is-correct)::before {
  content: "✓";
  position: absolute;
  left: 17px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 1rem;
}

:global(.price-option.is-wrong) {
  border-color: #b84a4a;
  background: rgba(184, 74, 74, 0.08);
  color: #9f3535;
  opacity: 1;
}

:global(.price-option.is-wrong)::before {
  content: "×";
  position: absolute;
  left: 17px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 1.2rem;
}

:global(.price-option.is-answer) {
  border-color: var(--good);
  background: rgba(30, 122, 79, 0.08);
  color: var(--good);
  opacity: 1;
  box-shadow: 0 0 0 3px rgba(30, 122, 79, 0.12);
}

:global(.price-option.is-answer)::before {
  content: "✓";
  position: absolute;
  left: 17px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 1rem;
}

      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 52px;
        padding: 0 20px;
        border: 0;
        border-radius: 14px;
        cursor: pointer;
        font-weight: 850;
      }

      .btn-primary { background: var(--accent); color: #fff; }
      .btn-secondary { background: #edf1f4; color: var(--ink); }
      .btn:disabled { opacity: .55; cursor: wait; }

      .result {
        display: none;
        margin-top: 18px;
        padding: 18px;
        border: 1px solid var(--line);
        border-radius: 18px;
        background: #faf9f7;
      }

      .result.is-visible { display: block; animation: reveal .32s ease both; }

      @keyframes reveal {
        from { opacity: 0; transform: translateY(7px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .result-kicker {
        margin: 0 0 4px;
        color: var(--muted);
        font-size: .72rem;
        font-weight: 850;
        text-transform: uppercase;
        letter-spacing: .08em;
      }

      .result-price {
        margin: 0;
        font-size: 2rem;
        line-height: 1;
        letter-spacing: -.04em;
      }

      .result-copy {
        margin: 10px 0 16px;
        color: var(--muted);
      }

      .result-actions {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 10px;
      }

      .loading {
        position: absolute;
        inset: 0;
        display: none;
        place-items: center;
        background: rgba(244,241,235,.84);
        backdrop-filter: blur(8px);
        z-index: 5;
        font-weight: 850;
      }

      .listing-photo.is-loading .loading { display: grid; }

      .error {
        display: none;
        margin-top: 16px;
        color: #a23939;
        font-weight: 700;
      }

      .error.is-visible { display: block; }

      @media (max-width: 820px) {
        .game { grid-template-columns: 1fr; }
        .listing-photo, .listing-photo img { min-height: 420px; }
        .panel { padding: 28px 22px 30px; }
      }

      @media (max-width: 520px) {
        .topbar { align-items: flex-start; }
        .scoreboard { gap: 5px; }
        .score-pill { min-width: 72px; padding: 8px; }
              .result-actions { grid-template-columns: 1fr; }
      }
    </style>
  </head>

  <body>
    <main class="shell">
      <header class="topbar">
        <a class="brand" href="/">bc.<span>realestate</span></a>
        <div class="scoreboard" aria-label="Game score">
          <div class="score-pill"><small>Round</small><strong id="roundValue">1 / 10</strong></div>
          <div class="score-pill"><small>Score</small><strong id="scoreValue">0</strong></div>
        </div>
      </header>

      <section class="game" aria-live="polite">
        <div class="listing-photo is-loading" id="listingPhoto">
          <div class="loading">Finding a home…</div>
          <img id="listingImage" alt="Active BC real estate listing" />
          <div class="photo-shade"></div>
         <div class="listing-caption">
  <h2 id="listingArea">Loading…</h2>
  <p id="listingType"></p>
</div>
        </div>

        <div class="panel">
          <p class="eyebrow">Real Estate IQ</p>
          <h1>Guess the asking price.</h1>
          <p class="intro">Study the home, make your call, and see how close you are. Every round uses a real active listing.</p>

          <div class="meta" id="listingMeta"></div>

         <div id="guessForm">
  <p class="price-question">Which is the asking price?</p>
  <div
    class="price-options"
    id="priceOptions"
    aria-label="Choose the asking price"
  ></div>
</div>

          <div class="result" id="resultCard">
            <p class="result-kicker">Listed for</p>
         <h3 class="result-price" id="actualPrice">$0</h3>
<p class="result-copy" id="resultCopy"></p>
<p class="result-copy" id="revealedAddress"></p>
            <div class="result-actions">
              <button class="btn btn-primary" id="nextButton" type="button">Next home</button>
              <a class="btn btn-secondary" id="viewListingButton" href="/explore">View listings</a>
            </div>
          </div>

          <p class="error" id="errorMessage"></p>
        </div>
      </section>
    </main>

    <script is:inline>
      const state = {
        listing: null,
        round: 1,
        maxRounds: 10,
        score: 0,
      };

      const $ = (selector) => document.querySelector(selector);
      const listingPhoto = $("#listingPhoto");
      const listingImage = $("#listingImage");
 const listingArea = $("#listingArea");
const listingType = $("#listingType");
const listingMeta = $("#listingMeta");
const priceOptions = $("#priceOptions");
const resultCard = $("#resultCard");
const actualPrice = $("#actualPrice");
const resultCopy = $("#resultCopy");
const revealedAddress = $("#revealedAddress");
      const nextButton = $("#nextButton");
      const errorMessage = $("#errorMessage");
      const roundValue = $("#roundValue");
      const scoreValue = $("#scoreValue");

      const money = new Intl.NumberFormat("en-CA", {
        style: "currency",
        currency: "CAD",
        maximumFractionDigits: 0,
      });

      function showError(message = "") {
        errorMessage.textContent = message;
        errorMessage.classList.toggle("is-visible", Boolean(message));
      }

      function updateScoreboard() {
        roundValue.textContent = `${state.round} / ${state.maxRounds}`;
        scoreValue.textContent = state.score.toLocaleString("en-CA");
      }

  

     function renderListing(listing) {
  state.listing = listing;

  const titleCase = (value) =>
    String(value || "")
      .replaceAll("_", " ")
      .replaceAll("-", " ")
      .replace(/\b\w/g, (letter) => letter.toUpperCase());

  const areaName = titleCase(listing.area) || "BC";
  const cityName = titleCase(listing.city);
  const typeName = titleCase(listing.type) || "Active listing";

  listingArea.textContent = [areaName, cityName]
    .filter(Boolean)
    .join(", ");

  listingType.textContent = typeName;

  listingImage.src = listing.image;
  listingImage.alt = `Active ${typeName.toLowerCase()} listing in ${areaName}`;

  const facts = [
    listing.beds ? `${listing.beds} beds` : "",
    listing.baths ? `${listing.baths} baths` : "",
    listing.sqft
      ? `${Number(listing.sqft).toLocaleString("en-CA")} sqft inside`
      : "",
    typeName,
  ].filter(Boolean);

  listingMeta.innerHTML = facts
    .map((fact) => `<span>${fact}</span>`)
    .join("");

  priceOptions.innerHTML = (listing.options || [])
    .map(
      (price) => `
        <button
          class="price-option"
          type="button"
          data-price="${Number(price)}"
        >
          ${money.format(Number(price))}
        </button>
      `
    )
    .join("");

  resultCard.classList.remove("is-visible");
  actualPrice.textContent = "$0";
  resultCopy.textContent = "";
  revealedAddress.textContent = "";
  showError();
}

      async function loadListing() {
        listingPhoto.classList.add("is-loading");
        const exclude = state.listing?.id ? `&exclude=${encodeURIComponent(state.listing.id)}` : "";

        try {
          const response = await fetch(`/api/price-game?city=nanaimo${exclude}`);
          const json = await response.json();
          if (!response.ok || !json.ok) throw new Error(json.error || "Could not load a listing.");
          renderListing(json.listing);
        } catch (error) {
          showError(error.message || "Could not load a listing.");
        } finally {
          listingPhoto.classList.remove("is-loading");
        }
      }

      priceOptions.addEventListener("click", async (event) => {
  const button = event.target.closest(".price-option");

  if (!button || !state.listing?.id) return;

  showError();

  const guess = Number(button.dataset.price);

  if (!Number.isFinite(guess) || guess <= 0) {
    showError("Choose one of the three prices.");
    return;
  }

  const buttons = Array.from(
    priceOptions.querySelectorAll(".price-option")
  );

  buttons.forEach((item) => {
    item.disabled = true;
  });

  try {
    const response = await fetch("/api/price-game", {
      method: "POST",
      headers: {
        "content-type": "application/json",
      },
      body: JSON.stringify({
        listingId: state.listing.id,
        guess,
      }),
    });

    const json = await response.json();

    if (!response.ok || !json.ok) {
      throw new Error(json.error || "Could not reveal the price.");
    }

    const result = json.result;

    state.score += result.points;
    updateScoreboard();

    buttons.forEach((item) => {
      const optionPrice = Number(item.dataset.price);

      if (optionPrice === result.askingPrice) {
        item.classList.add("is-answer");
      }
    });

    if (result.isCorrect) {
      button.classList.add("is-correct");
      resultCopy.textContent =
        `Correct. You earned ${result.points.toLocaleString("en-CA")} points.`;
    } else {
      button.classList.add("is-wrong");
      resultCopy.textContent =
        `Not quite. Your choice was ${money.format(result.difference)} ${result.direction}.`;
    }

    actualPrice.textContent = money.format(result.askingPrice);

    revealedAddress.textContent = result.address
      ? `${result.address} · ${[
          result.area,
          result.city,
        ].filter(Boolean).join(", ")}`
      : "";

    resultCard.classList.add("is-visible");

    nextButton.textContent =
      state.round >= state.maxRounds
        ? "Play again"
        : "Next home";
  } catch (error) {
    buttons.forEach((item) => {
      item.disabled = false;
    });

    showError(
      error.message || "Could not reveal the price."
    );
  }
});

      nextButton.addEventListener("click", async () => {
        if (state.round >= state.maxRounds) {
          state.round = 1;
          state.score = 0;
        } else {
          state.round += 1;
        }

        updateScoreboard();
        await loadListing();
      });

      updateScoreboard();
      loadListing();
    </script>
  </body>
</html>
