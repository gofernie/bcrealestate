---
import CityHomepage from "../../components/homepages/CityHomepage.astro";

export const prerender = false;

const city = String(Astro.params.city || "nanaimo")
  .toLowerCase()
  .trim();
---

<CityHomepage city={city} />