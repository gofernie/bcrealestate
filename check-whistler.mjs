import { createClient } from '@supabase/supabase-js'; import 'dotenv/config'; const sb = createClient(process.env.PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { data } = await sb.from('listing_rows').select('normalized_area, lat, lng').eq('normalized_city', 'whistler').eq('normalized_area', 'alpine-meadows').limit(5); console.log(JSON.stringify(data, null, 2));
